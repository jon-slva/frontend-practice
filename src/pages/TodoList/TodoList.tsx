import { useEffect } from "react"

// Your first task:

// Implement the data fetching logic inside the provided useEffect hook.
// Use JavaScript’s fetch API to get todos from your backend (http://localhost:4000/api/todos).
// Ensure fetched todos populate React state.

const TodoList: React.FC = () => {
  
  useEffect(() => {
    // Method 1 - fetch

  },[])

  return (
    <>
    <header>Simple Todo List (CRUD)</header>


    </>
  )
}

export default TodoList